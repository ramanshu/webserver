#include<stdio.h>

int main()
{
    int maxNumber,noToPrint;
    scanf("%d",&maxNumber);
    noToPrint = maxNumber;
    while(noToPrint>=1)
    {
                       printf("%d\n",noToPrint);
                       noToPrint = noToPrint - 1;
                       }
    
    }
